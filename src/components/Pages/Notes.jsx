import React from 'react';
import AddNote from '../Note_Components/AddNote';

const Notes = () => {
  return (
    <div className="notes-container">
      <AddNote />
    </div>
  );
};

export default Notes;
